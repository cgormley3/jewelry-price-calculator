import { League_Spartan } from "next/font/google";

/** League Spartan 900 — “THE VAULT” main header only. */
export const vaultHeaderFont = League_Spartan({
  subsets: ["latin"],
  weight: "900",
  display: "swap",
  adjustFontFallback: true,
});
