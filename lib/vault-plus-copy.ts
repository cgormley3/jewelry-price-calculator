/**
 * Vault+ pricing & mission copy — keep dollar amounts aligned with your Stripe Product price.
 * @see VAULT_PLUS_SETUP.md
 */

/** Short form for API paywalls, toasts, and inline parentheses. */
export const VAULT_PLUS_PRICE_PHRASE = '$12/year ($1/mo)';

/** Primary pricing line for modals and marketing. */
export const VAULT_PLUS_PRICING_HEADLINE =
  '$12 for the year — just $1 a month';

/** Nonprofit support line (BOMA) — shorter, for Vault/Compare panels. */
export const VAULT_PLUS_SUPPORT_COPY =
  'Helps support Boulder Metalsmithing Association, a 501(c)(3) nonprofit.';

/** Full sentence for the Vault+ unlock modal only. */
export const VAULT_PLUS_MODAL_SUPPORT_PARAGRAPH =
  'Supports Boulder Metalsmithing Association, a 501(c)(3) nonprofit, and unlocks the full suite of pricing tools below.';
